<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ordolabs.collect">

    <!-- TODO: implement 'allowBackup' -->
    <application
        android:name=".CollectApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CollectTheme.Light"
        tools:ignore="AllowBackup">

        <activity android:name="com.ordolabs.collect.ui.activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.CreateItemActivity" />

        <activity android:name=".ui.activity.EditItemActivity" />

    </application>

</manifest>